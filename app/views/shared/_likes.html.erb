<% pre_like = @post.likes.find { |like| like.user_id == current_user.id} %>
  <% if pre_like %>
    <%= link_to '💓', post_like_path(@post, pre_like), method: :delete %>
  <% else %>
    <%= link_to '💔', post_likes_path(@post), method: :post %>
  <% end %>
